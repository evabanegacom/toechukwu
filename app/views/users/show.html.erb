<p style="color: green"><%= notice %></p>

<%= render @user %>

<div>
  <%= link_to "Edit this guest", edit_user_path(@user) %> |
  <%= link_to "Back to guests", users_path %>

  <%= button_to "Remove this guest", @user, method: :delete %>
</div>
